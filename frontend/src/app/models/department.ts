export interface DepartmentRequest {
  deptCode: string;
  deptName: string;
  assignedHR: string;
}

export interface DepartmentResponse {
  deptCode: string;
  deptName: string;
  assignedHR: string;
}